# Prompt Template Strategy

- Prompts are versioned and stored in `docs/prompts.json`.
- Services only reference prompt keys and platform enums.
- Prompt updates should follow semantic versioning and changelog entries.
